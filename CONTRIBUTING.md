# Contributing

Thank you for your interest in the Secure FinTech Access & Audit Platform.

## Development Workflow

The repository uses the following long-lived branches:

- `main` contains stable and verified releases.
- `develop` is the integration branch for completed features.

All changes must be implemented in a dedicated branch created from `develop`.

Example:

```text
feature/access-request-domain